# 开发规范（清账小程序）

## 沟通规范
- **必须使用中文进行思考、回答。** 所有思考过程、回复、提交说明、代码注释一律用中文。

## 工程约定
- 前端：uniapp（Vue3）+ Pinia，主要目标端为微信小程序（mp-weixin）。
- 离线优先：所有记账先写本地缓存（秒回、断网可用），再后台增量同步（push 待发队列 / pull 增量）。
- 网络层统一在 `api/request.js`：业务错误用后端 msg，背景同步设 silent，用户操作给明确反馈。
- 后端地址在 `api/config.js`：小程序端走 `https://zzq323.top/bookkeeping`（Nginx 前缀，与微信合法域名一致）。
- 登录为手动触发（「我的」页微信登录按钮），失败弹框给出明确原因。
